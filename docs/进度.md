# 《家园》进度文档

> 本文档记录项目当前阶段、已完成功能（带证据）、遗留问题与待办清单。
> 任务开始前先查本节确认当前阶段与阻塞；任务完成后必须更新本文档的对应条目。

## 一、当前阶段

- **阶段定位**：核心玩法闭环（单机 v1.0 基础版）已成型 —— 资源网络、统一建筑、人口/食物值、政策转化、配方、加工、专精进化、幸福度、离线收益、存档迁移全部可用。
- **一句话状态**：单机可玩、逻辑完整；未实现战斗/怪物系统（PRD 概述中的远期目标），无测试框架，无后端（Supabase 未启动）。
- **下一任务**：见「四、待办清单」P1 产品决策项 —— 其中初始幸福度策略是剩余的具体体验决策。

## 二、已完成（带证据）

| 功能 | 证据（git commit / 代码位置） | 说明 |
| --- | --- | --- |
| 工程框架：Vue 3 + Vite + Pinia，深色主题单页 | `fa3bb7b` | `src/main.js`、`App.vue` 7 个 Tab 切换 |
| 统一建筑系统 | `fa3bb7b` | `BUILDINGS` 数组 + `type: production/processing/key`；独立物资容量（`getResourceCap`） |
| 离线收益 | `fa3bb7b` → 持续演进 | `calculateOffline()` 全额模拟，消耗速率 50%，上限 12 小时，弹窗展示 |
| 人口系统框架 | `9095c24`、`117dc61`、`5a68ef2` | 人口上限（关键建筑贡献）、槽位（`base + floor((level-1)/perLevel)`，Lv5 封顶）、派遣/撤回、人口 Tab |
| 食物系统重做 | `2b6d943` | `food` → `wheat`；需求系统（食物值）；政策系统（滑条转化，小麦 5:1、面包 1:1） |
| 存档迁移 | `511cb0b`、`2b6d943`、`ddc0d40` | v4/v5 → v6 自动迁移：food→wheat、补新资源、湖泊 Lv1、默认配方、清理残留派遣人口 |
| 供需平衡模型 | `ddc0d40` | 食物槽进度条、`getGrowthNeeded` 公式、赤字暂停增长 |
| 专精进化系统 | `40d7874`（含此前未提交工作） | 原建筑 Lv5 封顶 → A/B 分支二选一（替换制、不可重建、免费回退）、Lv10 封顶预留二次进化、`isEvolvedAway` 防重解锁 |
| 升级预览 | `40d7874`（含此前未提交工作） | `UpgradeModal` 二次确认：数值对比行 + 里程碑/配方解锁预告 |
| 配方系统 | `40d7874`（含此前未提交工作） | 多配方切换（`activeRecipes`）、多产出配方（`getBuildingRecipes` 兼容旧字段） |
| 加工系统增强 | `40d7874`（含此前未提交工作） | 多原料 `inputs` / 多产出 `outputs`、批次进度、原料不足/产物满仓暂停、Lv5+ 精炼二级成本 |
| 幸福度系统 | `40d7874` | 事件表（满足 +1 点、不满足收回，5 秒检查）、需求=⌊人口/2⌋、7 级状态修正人口增长与产出 |
| 聚集地被动幸福度 | 本次提交 | 建筑被动加成（`HAPPINESS_CONFIG.passiveBonuses`）：聚集地 Lv1 起常驻 +1 点，不走事件检查、回退即收回，UI 幸福度来源区展示 |
| 调试工具 | 随开发积累 | 设置 Tab：填满资源、各资源滑条（`setResourceAmount`/`fillAllResources`） |

## 三、遗留问题

1. ~~牧场/鱼塘数值偏差~~ **已解决**：用户拍板保持现状 —— `pasture` 兽肉/兽皮每级 +0.125/s、`fishPond` 淡水每级 +0.75/s 为既定数值；config.js 注释已改为如实描述，不再标注"偏差"，也不承诺"见开发文档"。
2. **新游戏初始即"不开心"（部分缓解，仍需产品确认）**：初始 4 人口、无事件 → 净幸福度 −2 → 不开心。已通过**聚集地被动 +1**（Lv1 起常驻）把初始净幸福度提升到 **−1**，但仍处于"不开心"档（产出 −5%、增长 −15%）。若产品意图是新手期中立，还需进一步调整（如初始事件、降低初始需求或提高事件点）；测试已按现状锁定（多处断言 ×0.95 与新增被动专项用例）。
3. **二次进化方案待定**：进化建筑 Lv10 封顶为"预留二次进化"，方案未设计（开发文档第 17 行注明）。
4. **CLAUDE.md 滞后**：组件清单此前缺 `UpgradeModal`/`EvolutionModal`，已同步（见 `40d7874` 后首次文档同步）。

## 四、待办清单

### P0 — 文档与代码同步（已完成）
- [x] 建立 `docs/进度.md`
- [x] 同步 CLAUDE.md 组件清单（补 UpgradeModal / EvolutionModal）
- [x] 清理 OfflineModal 中 `auto_` 里程碑自动产出的死代码

### P1 — 产品决策（阻塞对应功能的后续开发）
- [x] **牧场/鱼塘数值确认**：用户拍板保持现状（兽肉/兽皮 +0.125/s、淡水 +0.75/s），config.js 注释已改为如实描述
- [ ] **新游戏初始幸福度策略确认**：已加聚集地被动 +1（初始 −2 → −1，仍"不开心"档）；是否彻底中立（初始事件/调整需求/提高事件点）待定
- [ ] **战斗/怪物系统设计**：PRD 概述"抵御怪物侵袭"尚未实现；需出设计方案（是否本期做、建筑类型 `defense` 如何融入现有统一架构）
- [ ] **进化建筑二次进化方案**：Lv10 之后的进化方向与成本模型
- [ ] **是否启动 Supabase 后端**：开发文档第三章选型 Supabase，当前零后端代码；决定是否本期引入（影响存档结构与离线计算）

### P2 — 质量（已完成）
- [x] 引入测试框架（**Vitest 2.1.9 + @vue/test-utils 2.4.11 + jsdom 30**，用户拍板；版本约束：Vitest 锁 2.x 线，Vite 5 兼容，勿升 3.x）
- [x] 存档迁移测试（v4/v5 → v6 迁移路径用例，store.test.js）
- [x] 核心数值单测（`getGrowthNeeded`、`getUpgradeCost`、`getProductionRate`、加工批次模拟、专精进化、离线计算）
- [x] 组件冒烟测试（面板渲染、升级预览/专精弹窗交互）
- 现状：**75 个用例全部通过**（config 19 + store 48 + components 8）；`npm test` 运行

### P3 — 远期（PRD/代码注释预留的扩展点）
- [ ] 防御 / 探索建筑类型（`config.js` 第 38 行注释预留）
- [ ] 追加幸福度事件（`HAPPINESS_CONFIG.events` 扩展点，如 仓库兽肉、毛皮、大理石等）
- [ ] 更多加工链与精炼资源（当前 12 种精炼资源，诊所b/磨坊b/制皮坊b 等进化分支已占位）

## 五、阻塞

- 当前无代码层面阻塞。
- 主要阻塞为产品决策项（P1）：其中初始幸福度策略影响新手体验，建议优先确认；战斗系统、二次进化、Supabase 不影响当前单机闭环的继续打磨。
