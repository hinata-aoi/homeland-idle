# 《家园》 - 开发文档

## 一、游戏概述
- **游戏名称**：《家园》
- **一句话描述**：在荒野中建立家园——自动采集资源、经营加工链、抵御怪物侵袭，一款有策略深度的放置类生存游戏。
- **主题/世界观**：种田/生存 + 战斗元素
- **目标平台**：Web（手机浏览器 + 电脑浏览器）
- **目标用户**：喜欢放置类游戏、模拟经营、轻度战斗的休闲玩家

## 二、核心游戏机制

- **核心循环**：纯自动化放置 — 农田自动种植/收获，战斗自动进行。玩家只做升级决策（升级什么？解锁什么？资源怎么分配？）。离线也持续运转。
- **货币体系**：**物资网络 + 加工链**（非传统货币）。基础物资节点自动产出原材料（如：森林→木材、采石场→石头、农田→小麦、狩猎场→兽皮、湖泊→鱼+淡水），加工建筑将原材料转化为精炼物资（如 木材→木板、石头→石材、兽皮→皮革、小麦→面包）。升级和建造消耗这些物资而非单一货币。
- **成长系统**：**单级升级 + 专精进化（A/B分支）**。每级升级消耗资源递增、产量稳定增长（如 +10%/级）。所有生产/加工建筑 **Lv5 封顶**，达到 Lv5 后「升级」按钮变为「专精」，消耗资源（成本 = 原建筑 Lv5→Lv6 升级成本）二选一进化为全新建筑：
  - 进化 = **替换**：原建筑消失（不可重建），人口回到空闲池，新建筑从 Lv1 开始
  - 进化建筑不可直接建造，每个原建筑只能拥有一份进化版
  - 进化建筑 **Lv10 封顶**（预留二次进化，方案待定），可随时**免费回退**到原建筑 Lv1（进化建筑消失）
  - 生产进化建筑产出大增或改为多资源产出（如 农田→肥沃农场 8小麦/s 或 种植园 4小麦+4水稻/s）；加工进化建筑优化配方（如 磨坊→磨坊b 产面粉+纤维）
- **升级成本递进**：Lv1~4 只消耗基础资源（木材/石头），Lv5+ 额外消耗对应精炼物资（生产建筑需木板、加工建筑需石材），且成本曲线加速膨胀，形成中后期投资门槛。
- **升级预览确认**：点击升级先弹出「升级预览」界面，展示本次升级与当前的数值差异（产率、人口槽位、加工速度、原料消耗、里程碑解锁等）及升级成本，作为二次确认，避免误操作。
- **食物与人口系统**：人口不会饿死，食物值（食物槽）最低为 0，不会扣成负数；增长所需食物值阈值较高（基础公式 ×5），食物短缺时仅暂停人口增长，充足后自动恢复。市政厅 Lv1 起被动产出食物值（2/s），为人口兜底。
- **政策转化**：可将资源按比例转化为食物值（如 小麦 5:1、面包 1:1），政策随对应建筑等级解锁，转化速率可调。
- **幸福度系统**：通过事件表获得/收回幸福度点数。事件满足条件时 +1 幸福度（如 仓库淡水 ≥ 人口×200、仓库酒 ≥ 人口×20），条件不再满足时收回。另有**建筑被动加成**（`HAPPINESS_CONFIG.passiveBonuses`，如 聚集地 Lv1 起常驻 +1 点）：等级满足即生效、回退即收回，**不经过 5 秒事件检查**（即时生效）。每 2 人口需要 1 点幸福度需求（1人0点、2人1点、4人2点），幸福度−需求 的差值决定全局状态：
  - +3 以上 狂喜：人口增长 +20%、所有建筑产出 +10%
  - +1~+2 高兴：人口增长 +10%、所有建筑产出 +5%
  - 0 满意：中立无效果
  - -1~-2 不开心：人口增长 -15%、建筑产出 -5%
  - -3~-4 生气：人口增长 -30%、建筑产出 -10%
  - -5~-6 不安：人口增长 -100%、建筑产出 -30%
  - -7 以下 反感：人口增长 -100%、建筑产出 -60%
  - 人口增长修正作用于食物值盈余速率（如盈余 +4.00/s、+10% 增长时等效 +4.40/s），仅修正盈余不放大赤字；建筑产出修正同时作用于生产建筑产出率与加工建筑处理速度。事件每 5 秒检查一次，幸福度面板位于政策 Tab 内的「幸福度」子 Tab（与「食物发放」并列）。
- **远征系统**：玩家**主动发起**的战斗玩法（非被动怪物入侵）。市政厅 Lv3 解锁**公会**（key 建筑，关键 Tab 内，Lv5 封顶），公会等级决定**唯一远征队伍**的战力（Lv1 武装镇民 10 → Lv2 民兵 30 → Lv3 卫兵 60 → Lv4 骑士 100 → Lv5 精锐 160），队伍不占人口、同一时间只能进行一个远征。远征 Tab 内选择地图派遣（免费），按「队伍战力 ÷ 地图战力要求」分 4 档结算（<50% 失败无奖励 / [50%,75%) / [75%,100%) / ≥100% 全胜），**每档固定奖励表 + 随机掉落**；**奖励在出发时锁定**，按真实时间倒计时（离线照常计时），到点自动发放，**中途取消无奖励**。地图按进度解锁：**全胜（≥100% 档）上一张后开放下一张**（草原→丘陵→森林深处→雪山→荒漠，时长 2/4/8/12/16 小时，要求 15/40/80/130/200）。战力强化（如铁匠铺产铁剑 → 下次远征 +20% 战力）预留 `EXPEDITION_BUFFS` 配置位，本次未实现 UI。
- **货币与税所系统**：**金币**为独立货币——不占仓库容量、**无上限**，顶部 Header 单独显示（🪙）。市政厅 Lv4 解锁**税所**（key 建筑，关键 Tab 内，**最高 3 级、无专精化**）。政策面板新增「税务」子 Tab，可选征税档位：按**总人口（含空闲）**每 **30 分钟结算一轮金币**（真实时间，离线照常累计、加载时补发），换档重置计时；**档位幸福度修正为常驻**（换档立即生效，可正可负）。档位表按税所等级（`TAX_CONFIG.tiersByLevel`）：
  - Lv1 四档：不征税(0 金币 / 幸福度 **+1**)、轻税(3 / 0)、常规税(5 / −1)、重税(9 / −3)
  - Lv2：除不征税外三档金币各 +1（4/6/10）
  - Lv3：除不征税外三档幸福度各 +1（4/+1、6/0、10/−2）并新增第 5 档「重税+」(15 / −4)
  - 商人交易（金币消费）待后续开发，当前金币只进不出。
- **离线机制**：**全额离线 + 每种物资独立存储上限**。离线期间所有节点100%运转产出，但每种物资拥有独立的存储上限（基础上限因物资类型而异）。若某物资堆积达到上限，该物资的溢出部分被浪费，不影响其他物资的存储。仓库升级为所有物资统一增加容量。玩家需定时上线消耗/加工物资以腾出空间。
- **建筑系统**：**统一建筑架构**。所有建筑归属于 `BUILDINGS` 数组，通过 `type` 字段区分：
  - `production`（生产建筑）：随时间自动产出资源，可从1级开始升级；部分生产建筑通过市政厅等级解锁（如 采石场 Lv2、狩猎场 Lv3）
  - `processing`（加工建筑）：将资源转换为其他资源，由市政厅里程碑解锁（初始0级=未解锁），解锁后可独立升级以提高加工效率。支持单原料（`input`）或多原料（`inputs` 数组，如诊所消耗 2草药+1淡水）、单产出（`output`）或多产出（`outputs` 数组，如锯木厂b 产 0.5木板+0.5树脂），任一原料不足或任一产物满仓时加工暂停
  - `evolvedOnly` 建筑（专精进化产物）：不可直接建造、不出现在锁定面板，仅通过原建筑 Lv5 专精获得
  - `key`（关键建筑）：提供全局加成（人口上限、解锁建筑、被动食物），无需进驻人口
  - 未来可扩展更多建筑类型（防御、探索等）
- **生产配方**：生产建筑可配置 `recipes` 配方列表，玩家在已解锁配方间切换（如农田 Lv5 起可选小麦/水稻）；单个配方可同时产出多种资源（如湖泊：0.25鱼/s + 1淡水/s）。缺省配方由旧的 `produces`/`baseRate` 字段自动推导，旧建筑无需修改
- **人口槽位上限**：建筑每级增加人口槽位，但 **Lv5 后不再增加**（除非建筑单独说明），避免无限堆人力。
- **加工建筑定位**：升级成本使用石头而非木材，初始成本更高、成长更陡（起价 60~120 石头），定位为"昂贵且强大"的中后期投资；随等级提升加工速度每级 +12%，原料消耗逐步降低。

## 三、技术栈决策

### 前端
- **选择**：Vue 3 + Vite
- **理由**：学习曲线友善、中文社区强大、组件化适合后续扩展

### 后端 / 部署
- **选择**：Supabase（开源云数据库 + 用户系统 + 实时同步）
- **部署平台**：Vercel / Netlify（静态托管，免费）
- **理由**：内置认证系统、PostgreSQL 数据库、原生支持实时数据同步，从单人到多人在线都可以无缝扩展，国内可访问，开源可自建

### 测试框架
- **选择**：Vitest 2.x + @vue/test-utils + jsdom（2025-03 由用户拍板，候选对比见 git 提交说明）
- **理由**：与 Vite 5 同一引擎原生集成（复用 vite.config.js，零桥接）；原生 ESM 支持；Vue/Pinia 官方文档示例即 Vitest；弃选 Jest（需额外插件桥接 Vite/ESM、配置繁琐）与 node:test（无法编译 .vue 组件）
- **版本约束**：**Vitest 必须锁 2.x 线**（与 Vite 5 兼容）；Vitest 3 最新版已移除 Vite 5 支持，升级 Vite 前不得升 Vitest 3
- **命令**：`npm test`（vitest run 单次）/ `npm run test:watch`（监听模式）
- **测试分层**：`src/game/config.test.js` 纯函数 → `src/game/store.test.js` store 集成（tick/离线/迁移/进化）→ `src/components/panels.test.js` 组件冒烟（Teleport 弹窗断言用 document.body）

## 四、项目协作规则
- 所有技术决策由开发者（Claude Code）列出方案，用户（产品方）拍板
- 用户不懂技术，开发者必须用通俗语言解释每个选项
- 每次技术讨论后必须更新本文件
- 禁止开发者自行决定技术选型

## 五、代码架构参考

> 本章内容原位于 CLAUDE.md，为保持 CLAUDE.md 精简而移入此处。Claude Code 在处理代码任务时可参考本章。

### config.js — 配置层

- `BUILDINGS` 数组：统一定义所有建筑，通过 `type: 'production' | 'processing' | 'key'` 区分类型
- 三个派生常量：`PRODUCTION_BUILDINGS` / `PROCESSING_BUILDINGS` / `KEY_BUILDINGS`（filter 自动生成）
- `BASIC_RESOURCES` / `REFINED_RESOURCES`：每种资源有 `starting`（初始量）和 `baseCapacity`
- `getBuilding(id)` 和 `getGrowthNeeded(n)` 为导出工具函数
- 建筑字段约定：
  - `populationSlots: { base, perLevel }` — 槽位 = `base + floor((level-1)/perLevel)`
  - `unlockBy: { building, level }` — 解锁条件（生产/加工建筑初始 level 0 时使用；UI 显示"XX LvN 解锁"）
  - `milestones: { level: { desc, bonus, unlockBuilding?, unlockBuildings? } }` — 里程碑效果（**里程碑系统重做后仅关键建筑使用**：市政厅 Lv2/Lv3 解锁建筑、聚集地 Lv5/Lv10 人口加成；生产/加工建筑已无里程碑，改为 Lv5 专精进化）
  - `recipes: [{ id, name, outputs: [{ resource, rate }], unlockAt? }]` — 生产配方（可多配方切换，`unlockAt` 为配方解锁等级）；缺省时由旧字段 `produces`/`baseRate` 自动推导单一配方（`getBuildingRecipes`）
  - `maxLevel` — 封顶等级，达到后升级按钮变"已满级"且 `upgradeBuilding` 拒绝升级。原生产/加工建筑统一 **Lv5**（升级按钮变"专精"）；进化建筑统一 **Lv10**（预留二次进化）
  - `evolvedOnly: true` — 进化专用建筑：不可直接建造，不显示在锁定面板；仅能通过原建筑专精获得
  - `evolvedFrom: '原建筑id'` — 该进化建筑的原建筑，用于回退还原与"不可重建"判断
  - `input: { resource, amount }` — 加工建筑单原料消耗；或用 `inputs: [{ resource, amount }]` 声明多原料（如诊所：2草药+1淡水）
  - `output: { resource, amount }` — 加工建筑单产出；或用 `outputs: [{ resource, amount }]` 声明多产出（如锯木厂b：0.5木板+0.5树脂）
  - `maxPopBase` / `maxPopPerLevel` — 关键建筑的人口上限贡献
- `HAPPINESS_CONFIG`：幸福度配置——`events` 事件表（每项含 `id/name/description/icon/points` 与 `check(state)` 条件函数，满足条件时 +points，不再满足收回；后续事件在此追加）、`passiveBonuses` 建筑被动加成列表（每项含 `building/level/points`，建筑等级 ≥ level 即常驻获得 points，等级回退即收回，不走事件检查）、`statusLevels` 状态层级（按 `minNetPoints` 降序，含 `growthMod` 人口增长修正与 `outputMod` 产出修正）、`eventCheckInterval` 事件检查间隔（秒）；配套导出 `getHappinessStatus(netPoints)` 状态匹配函数
- `GUILD_CONFIG`：公会/远征队伍配置——`maxLevel`（Lv5 封顶）、`teamPowerByLevel`/`teamNameByLevel`（公会等级 → 唯一队伍的战力与名称）
- `EXPEDITION_MAPS`：远征地图表——每项含 `id/name/icon/description/powerRequirement/durationSec/unlockAfter?/rewards`；`rewards` 为 4 档（`lt50`/`50-75`/`75-100`/`gte100`）固定奖励表 `{ resource: [min, max] }` 随机范围；`unlockAfter` 为前置地图 id（全胜后解锁），首张图无此字段；配套导出 `getExpeditionMap(id)`
- `EXPEDITION_BUFFS`：远征战力强化**预留位**（空数组）——未来每项 `{ id, name, description, powerBonusPercent, cost: { resource, amount } }`（如铁匠铺铁剑 → 下次远征战力 +20%）
- `TAX_CONFIG`：税所/货币配置——`settlementIntervalSec`（金币结算周期，30 分钟）、`tiersByLevel`（税所等级 → 档位表，每档 `{ id, name, goldPerPerson（每轮每人金币）, happiness（常驻幸福度修正） }`）；配套导出 `getTaxTiersByLevel(level)`

### store.js — 状态层

使用 Pinia **setup store** 语法（`defineStore('game', () => { ... })`），所有状态用 `ref()`，派生值用 `computed()`。

**关键状态变量**：
- `resources` / `refined` — 基础/加工资源存量
- `buildingLevels` — `{ buildingId: level }`，加工建筑和部分生产建筑 level 0 = 未解锁
- `activeRecipes` — `{ buildingId: recipeId }`，各生产建筑当前活跃配方
- `totalPopulation` — 独立 `ref()`，不是 computed
- `populationAssigned` — `{ buildingId: count }`
- `foodValue` — 食物槽（既是存量也是进度条）
- `policyRates` — `{ resourceId: ratePerSec }`
- `activeHappinessEvents` — 当前激活的幸福度事件 id 列表（`happinessPoints` 为其点数总和 computed）

**游戏循环** (`tick()`, 1000ms interval)：
1. 政策转化：资源 → 食物值
2. 生产建筑产出（有驻人时，乘幸福度产出倍率 `happinessOutputMultiplier`）
3. `foodValue -= totalPopulation × 2` （每人每秒消耗 2 食物值）
4. 幸福度增长修正：对净食物值盈余乘 `happinessGrowthMultiplier`（仅盈余为正时生效）
5. `while foodValue >= getGrowthNeeded(pop)` → 人口 +1
6. 每 `HAPPINESS_CONFIG.eventCheckInterval` 秒调用一次 `checkHappinessEvents()` 检查事件激活状态

**幸福度系统**：`happinessDemand = floor(totalPopulation/2)`、`happinessNetPoints = happinessPoints - happinessDemand`、`happinessStatus = getHappinessStatus(happinessNetPoints)`；产出倍率经 `getProductionRate(b, level, assigned, recipe, happinessMult)` 参数与加工速率计算应用，`processBuildingPerSecond` 与离线计算（`calculateOffline`）同样受修正；事件状态随存档保存（`activeHappinessEvents`）。

**离线计算** (`calculateOffline()`)：完整模拟 tick，消耗速率 50%。在 `load()` 末尾自动调用。

**解锁机制**：
- `checkUnlocks()` 遍历 production + key 建筑，检查里程碑的 `unlockBuilding`（单个）或 `unlockBuildings`（数组），将目标建筑从 0 设为 1；已被进化掉的建筑（`isEvolvedAway`）跳过，不会被重新解锁
- 市政厅 Lv2 里程碑：`unlockBuildings: ['sawmill', 'mason', 'mill', 'quarry', 'deepMountain', 'clinic']`；Lv3 里程碑：`unlockBuildings: ['hunting', 'tannery']`
- 在 `initNewGame()`、`upgradeBuilding()`、`load()` 中调用

**配方系统**：
- 生产建筑在 `tick()` / `calculateOffline()` 中按活跃配方产出（`getActiveRecipe`），一次可产出多种资源（如湖泊：鱼+淡水）
- `getProductionRate(building, level, assigned, recipe)` 返回 `[{ resource, ratePerSec }]` 数组
- 切换配方：`setActiveRecipe(buildingId, recipeId)`（`activeRecipes` 状态）

**加工建筑**（`processBuildingPerSecond()`，实时与离线共用）：`getInputs(building)` 统一取原料列表（兼容单 `input` 与多 `inputs` 数组），`getOutputs(building)` / `getEffectiveOutputs(building, level)` 统一取产出列表（兼容单 `output` 与多 `outputs` 数组）；`getEffectiveInputAmount(building, level, inputDef)` 计算单种原料有效消耗量；每批循环检查并消耗所有原料、检查所有产出容量，任一不满足则进度暂停。`unlockedProcessingBuildings` 计算属性提供 `inputsAmount`（各原料库存/需求量）、`allInputsMet`、`inputSummary`、`outputsAmount`（各产物库存/容量）、`outputsFull`、`outputSummary` 供 UI 展示。

**maxLevel 封顶**：建筑可配 `maxLevel`，`upgradeBuilding()` 在 `level >= maxLevel` 时返回 false。原生产/加工建筑满级时 UI 显示"专精"按钮（生产/加工两面板），进化建筑满级显示"已满级"。

**专精进化系统**：
- `evolutionChoice` computed：打开专精弹窗时提供原建筑信息、进化成本（`getUpgradeCost(原建筑, maxLevel)` 即 Lv5→Lv6 成本）及两个分支的展示摘要
- `evolveBuilding(fromId, toId)`：校验原建筑满级、目标 `evolvedFrom` 匹配、原建筑未被其他分支占用 → 扣费 → 释放人口/清除加工进度 → 原建筑置 0（进化消失）、进化建筑置 1（含默认配方）→ 加入 discovered
- `revertBuilding(evolvedId)`：免费回退——进化建筑置 0，原建筑回到 Lv1
- `isEvolvedAway(buildingId)`：若存在任一已建造的进化建筑 `evolvedFrom` 指向某原建筑，则该原建筑视为"已进化消失"——锁定面板不显示、`checkUnlocks()` 不会重新解锁（防止市政厅里程碑把已进化的深山/锯木厂等重新解锁）
- 进化建筑初始等级 0（`initNewGame` 中 `evolvedOnly` 的建筑不默认解锁）

**存档**：key `homeland_save_v8`，`save()` 在每次操作和每 30 秒自动调用。`load()` 兼容旧 `homeland_save_v7`/`homeland_save_v6`/`homeland_save_v5`/`homeland_save_v4`（自动迁移并写入 v8 键：补齐新资源、湖泊 Lv1、默认配方、activeRecipes 等），并清理已删除建筑在 `populationAssigned` 中的残留派遣人口。v7 新增远征状态（`expedition` 当前远征、`completedMaps` 全胜记录）；v8 新增货币/税务（`gold` 金币、`taxRate` 档位、`lastTaxCollectTime` 结算时间戳）。`load()` 末尾对已超时的远征立即结算、对超期税轮补发金币（均按真实时间，离线照常累计）。旧版 Lv15 里程碑加成（产量加成、autoProduce、extraOutput）已随里程碑系统重做移除。

**远征系统**：`expedition` 状态为单条记录（唯一队伍）：`{ mapId, startTime, power, tier, rewards, durationSec }`——出发时锁定战力/档位/奖励（`getExpeditionTier` + `rollExpeditionRewards`）；`checkExpeditionCompletion()` 在 `tick()` 与 `load()` 中调用，到期发奖（`addResource` 容量截断），`gte100` 档记录 `completedMaps` 用于解锁下一张（`isMapUnlocked`）。`startExpedition` 校验：远征中/公会未解锁（战力 ≤ 0）/地图未解锁均拒绝；`cancelExpedition` 直接清空（无奖励）。UI 由 `ExpeditionPanel.vue` 提供（队伍总览、进行中远征倒计时、地图列表与档位奖励预览）。

**税所/货币系统**：`gold` 独立货币（不占仓库、无上限，Header 展示）；`taxRate` 为当前档位 id，`lastTaxCollectTime` 为上次结算时间戳。`taxTiers`/`currentTaxTier`/`taxHappinessModifier` 由税所等级 + 档位查表（`getTaxTiersByLevel`），`happinessPoints` 计入 `taxHappinessModifier`（常驻修正）。`setTaxRate` 校验档位存在并**重置结算计时**（防切档卡结算）；`checkTaxCollection()` 在 `tick()` 与 `load()` 中调用，每 `settlementIntervalSec`（30 分钟）按 `总人口 × 档位金币` 发放一轮、支持多轮累计，`goldPerPerson ≤ 0`（不征税）不结算。UI 由 `PolicyPanel.vue`「税务」子 Tab 提供（档位单选、轮收益预览、下一轮倒计时）。

### 组件模式

所有面板组件遵循相同模式：
1. 从 store 获取 computed 列表（如 `store.productionBuildings`）
2. `v-for` 渲染 `.panel` 卡片
3. 卡片结构：标题行（图标+名称+等级+标签）→ 类型特定详情 → 里程碑预告 → 升级按钮

**标签页切换**：App.vue 使用 `v-if`（非 `v-show`），切换时组件销毁重建。
